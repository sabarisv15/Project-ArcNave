# Approved Spec — Template

The implementation-ready artifact `/build-slice` and `/wire-frontend`
consume. Written only after Step 12 (Product Refinement) resolves — either
immediately (no threshold-met item found) or after the rare answered
question. See [`00-workflow.md`](00-workflow.md) §16.

**This document's OUT OF SCOPE section is a hard implementation boundary**:
`/build-slice` and `/wire-frontend` must not implement, wire, refactor, or
change anything listed there unless a new, separate Product Reasoning pass
explicitly brings it into scope.

---

## Page

## Purpose

## Role

## Navigation

## Tabs

## Features

List each `CORE` / `REQUIRED SUPPORT` feature approved for implementation,
linking to its feature contract.

## User flows

## UI components

## Permissions

## API contracts

Request/response shape for each endpoint this spec requires (new or
existing).

## Data dependencies

## States

Loading / empty / error / success.

## Validation

## Edge cases

## Testing requirements

## OUT OF SCOPE

Every `RELATED / FUTURE`, `EXISTING CAPABILITY / RELATED / UNWIRED`, and
`FUTURE` item found while analyzing this page/feature, with its
classification. Nothing here is implemented by this spec.

| Item | Classification | Notes |
|---|---|---|
